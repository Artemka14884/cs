# Arena FPS Online

3D онлайн-шутер в браузере (Three.js + Socket.io) в стиле CS:
- **AK-47, пистолет и нож** — три вида оружия с переключением на `1`/`2`/`3` (или кнопкой на телефоне): у автомата и пистолета свой урон, скорострельность и паттерн отдачи (у пистолета — заметно слабее), нож — ближний бой с высоким уроном и без патронов.
- **Гранаты**: бросаются клавишей `G` (или кнопкой 💣 на телефоне), летят по дуге с гравитацией, взрываются через 1.5 сек с уроном по расстоянию (задевает и себя при близком взрыве). Подбираются на карте — зелёные восьмигранники разбросаны в случайных точках и возрождаются через 14 сек после подбора.
- **Бесконечные патроны**: магазин перезаряжается (клавиша R) сколько угодно раз, резерв никогда не заканчивается.
- **Баннихоп**: физика движения в стиле Source-engine — трение не применяется в момент прыжка, в воздухе разрешено ускорение через strafe (A/D + мышь), что позволяет набирать скорость сериями прыжков.
- **Три карты на выбор** (выбирается в меню перед входом, карту сессии задаёт первый подключившийся игрок):
  - **Тренировочная арена** — укрытия, стена-мишень для spray-паттерна, зона с рампами для баннихопа, бочки и ящики.
  - **Пустынный рынок** — оригинальная карта в духе ближневосточного города (песочные здания, арки, рыночная площадь, переулки, башни, пальмы). Авторская карта, вдохновлённая атмосферой пустынных карт жанра, а не копия официального левел-дизайна.
  - **Промзона** — контейнерный порт: лабиринт из штабелей контейнеров разных цветов, портовые краны, ящики, металлические рампы под баннихоп.
- **Исправлены коллизии**: у вложенных объектов (прилавки на рынке, арки) отталкивание раньше считалось от локальных координат меша внутри группы вместо мировых — из-за этого казалось, что коллизии нет вообще. Теперь расчёт идёт от мирового центра bounding box, и все объекты на всех картах блокируют проход корректно.
- **Мультиплеер**: сервер на Node.js (Express + Socket.io) синхронизирует позиции игроков, обрабатывает попадания, урон, смерти/респавн и общий чат. Работает с любым числом игроков, у каждого — своё оружие и своя отдача, независимо считаемая на его клиенте.

## Управление
### Компьютер
- `WASD` — движение
- Мышь — обзор
- ЛКМ (удерживать) — стрельба/удар ножом
- `1` / `2` / `3` — переключение АК-47 / Пистолет / Нож
- `G` — бросить гранату
- `Пробел` — прыжок (для баннихопа прыгайте сразу в момент касания земли и продолжайте зажимать A/D, слегка покачивая мышью в стороны)
- `R` — перезарядка
- `Shift` — бег по земле
- `T` — чат
- `Tab` — таблица игроков
- `Esc` — отпустить курсор

### Телефон / планшет
Управление определяется автоматически при заходе с сенсорного устройства — появляется отдельный интерфейс:
- **Джойстик слева внизу** — движение.
- **Свайп по правой половине экрана** — обзор камерой.
- **Большая круглая кнопка справа** — стрельба/удар ножом (можно удерживать для очереди).
- **Кнопка ↑** — прыжок.
- **Кнопка R** — перезарядка.
- **Кнопка 🔫** — циклическое переключение оружия (АК-47 → Пистолет → Нож).
- **Кнопка 💣** — бросить гранату.

Интерфейс и HUD адаптируются под маленький экран автоматически.

## Запуск локально

```bash
npm install
npm start
```

Открыть в браузере: http://localhost:3000

Для игры вдвоём и более — откройте эту же ссылку на другом устройстве в той же сети (замените localhost на IP машины), либо задеплойте на render.com (ниже) и делитесь публичной ссылкой.

## Деплой на Render.com

### Вариант 1 — через Blueprint (render.yaml), самый быстрый
1. Залейте эту папку в репозиторий на GitHub (или GitLab).
2. Зайдите на https://dashboard.render.com → **New** → **Blueprint**.
3. Укажите репозиторий — Render сам прочитает `render.yaml` и создаст веб-сервис.
4. Нажмите **Apply**. Через 1–2 минуты сервис будет доступен по адресу вида `https://arena-fps-online.onrender.com`.

### Вариант 2 — вручную
1. **New** → **Web Service** → подключите репозиторий.
2. Настройки:
   - **Runtime**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free (или выше для стабильного онлайна без "засыпания")
3. Deploy.

Сервер сам слушает порт из переменной окружения `PORT`, которую Render передаёт автоматически — ничего дополнительно настраивать не нужно.

## Известное ограничение
Точки подбора гранат считаются локально на каждом клиенте (не синхронизируются через сервер), поэтому в редких случаях два игрока могут почти одновременно подобрать одну и ту же гранату независимо друг от друга — на геймплей это не влияет, но это стоит знать.

## Структура проекта
```
├── server.js          # Node.js сервер: Express + Socket.io, логика игроков/урона/респавна
├── package.json
├── render.yaml         # Blueprint-конфиг для Render.com
└── public/
    ├── index.html       # Меню, HUD
    ├── style.css
    └── client.js         # Three.js: сцена, карта, физика/баннихоп, AK-47, сеть
```

## Технические детали отдачи и баннихопа

**Отдача AK-47** (`client.js`, блок `AK_RECOIL_PATTERN`): заранее просчитанный паттерн из 30 патронов — первые выстрелы уводят прицел строго вверх, затем добавляется случайное расхождение вправо-влево с нарастающей амплитудой (имитация реального "спрея" автомата). Смещение накапливается в `recoilOffsetPitch/Yaw` и плавно затухает через `updateRecoilRecovery`, если пауза между выстрелами больше 180 мс.

**Баннихоп** (`client.js`, блок `updateMovement`): при прыжке трение (friction) не применяется к горизонтальной скорости — она полностью переносится в полёт. В воздухе действует ограниченный `wishSpeed`, но ускорение (`accelerate`) не срезает уже набранную скорость, поэтому постоянные strafe-прыжки (A/D + покачивание мыши) позволяют разгоняться выше базовой скорости бега — классическая механика бхопа.
